/**
 * Barrel del protocolo de red. Permite importar `@shared/protocol` (la resolución
 * por alias a un directorio busca su index). Skill `authoritative-netcode`.
 */
export * from './messages';
export * from './wire';
