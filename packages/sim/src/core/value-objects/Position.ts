/**
 * `Position` es un `Vec3` (un punto 3D). Se re-exporta con nombre semántico para
 * legibilidad; es la MISMA clase, así posiciones y velocidades comparten una única
 * hidden class (monomórfico). Skill `workers-memory-optimization`.
 */
export { Vec3 as Position } from './Vec3';
