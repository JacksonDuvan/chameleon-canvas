/**
 * Environment — el escenario: superficies con materiales/patrones llamativos
 * (ladrillo, madera…) de los que el Hider absorbe color/textura con el raycaster.
 * También define la luz (clave para el "match de sombras": una sombra mal calculada
 * delata al Hider congelado).
 *
 * Skill `r3f-rendering`: materiales/geometrías memoizados y compartidos.
 * SCAFFOLD del Paso 1 — Paso 4.
 */
export function Environment() {
  return null;
}
