/**
 * PlayerList — jugadores en la sala y su rol (Hider/Seeker). Lee estado LENTO del
 * worldStore con selector reactivo (seguro: cambia rara vez). Skill `r3f-rendering`.
 *
 * SCAFFOLD del Paso 1 — Paso 4.
 */
export function PlayerList() {
  return null;
}
