/**
 * RoomForm — crear/unirse a una sala (lobby). UI pura: consume lógica vía hooks
 * (api RPC + useGameSockets), no contiene reglas de negocio (skill
 * `hexagonal-vertical-slicing`, separación estricta UI/lógica).
 *
 * SCAFFOLD del Paso 1 — Paso 3/4.
 */
export function RoomForm() {
  // TODO: form accesible (label/input), submit -> api.api.gameplay.rooms.$post(...)
  return null;
}
