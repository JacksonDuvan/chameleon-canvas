{
  "name": "@mecha/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Worker autoritativo del juego: Hono + Durable Objects (GameRoomDO). Exporta AppType para el tipado RPC end-to-end del frontend.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run -c vitest.config.ts",
    "test:do": "vitest run -c vitest.workers.config.ts",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@mecha/shared": "workspace:*",
    "@mecha/sim": "workspace:*",
    "hono": "catalog:",
    "@hono/zod-validator": "catalog:",
    "zod": "catalog:",
    "@dimforge/rapier3d-compat": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@cloudflare/vitest-pool-workers": "catalog:",
    "vitest": "catalog:",
    "vite-tsconfig-paths": "catalog:",
    "wrangler": "catalog:",
    "typescript": "catalog:"
  }
}
